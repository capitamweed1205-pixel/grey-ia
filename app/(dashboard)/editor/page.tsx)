"use client"
import { useState } from 'react';

export default function EditorPro() {
  const [mode, setMode] = useState('photo'); // 'photo' o 'video'
  const [loading, setLoading] = useState(false);

  return (
    <div style={{ padding: '25px', backgroundColor: '#000', minHeight: '100vh', color: '#fff', fontFamily: 'sans-serif' }}>
      <header style={{ marginBottom: '30px' }}>
        <h2 style={{ fontSize: '26px', fontWeight: '900', fontStyle: 'italic', margin: '0' }}>GREY_STUDIO</h2>
        <p style={{ color: '#555', fontSize: '10px', fontWeight: 'bold', letterSpacing: '2px' }}>AI_POST_PRODUCTION_SUITE</p>
      </header>

      {/* SELECTOR DE MODO */}
      <div style={{ display: 'flex', gap: '10px', marginBottom: '25px' }}>
        <button onClick={() => setMode('photo')} style={mode === 'photo' ? activeTab : inactiveTab}>EDITOR_FOTO</button>
        <button onClick={() => setMode('video')} style={mode === 'video' ? activeTab : inactiveTab}>EDITOR_VÍDEO</button>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>
        
        {/* ÁREA DE TRABAJO / PREVIEW */}
        <div style={{ height: '300px', backgroundColor: '#0a0a0a', border: '1px solid #1c1c1c', borderRadius: '20px', display: 'flex', alignItems: 'center', justifyContent: 'center', position: 'relative', overflow: 'hidden' }}>
          <div style={{ textAlign: 'center', color: '#222' }}>
             <p style={{ fontSize: '12px', fontWeight: '900' }}>{mode === 'photo' ? 'SOLTAR_IMAGEN_AQUÍ' : 'SOLTAR_VÍDEO_AQUÍ'}</p>
             <button style={{ marginTop: '10px', background: 'none', border: '1px solid #222', color: '#444', padding: '8px 15px', borderRadius: '5px', fontSize: '10px' }}>CARGAR_ARCHIVO</button>
          </div>
        </div>

        {/* HERRAMIENTAS IA */}
        <div style={{ backgroundColor: '#0a0a0a', border: '1px solid #1c1c1c', padding: '20px', borderRadius: '20px' }}>
          <p style={{ fontSize: '9px', color: '#555', fontWeight: 'bold', marginBottom: '15px', letterSpacing: '1px' }}>HERRAMIENTAS_DE_MAGIA_IA</p>
          
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '10px' }}>
            {mode === 'photo' ? (
              <>
                <button style={toolBtn}>REMOVER_FONDO</button>
                <button style={toolBtn}>RE-ILUMINACIÓN</button>
                <button style={toolBtn}>FILTRO_CINEMATIC</button>
                <button style={toolBtn}>MAG_EXPAND</button>
              </>
            ) : (
              <>
                <button style={toolBtn}>AUTO_CORTE_IA</button>
                <button style={toolBtn}>LIMPIAR_AUDIO</button>
                <button style={toolBtn}>COLOR_GRADE</button>
                <button style={toolBtn}>SLOW_MOTION_PRO</button>
              </>
            )}
          </div>

          <button 
            onClick={() => { setLoading(true); setTimeout(() => setLoading(false), 3000); }}
            style={{ width: '100%', marginTop: '20px', backgroundColor: '#fff', color: '#000', padding: '15px', borderRadius: '12px', fontWeight: '900', border: 'none', fontSize: '12px' }}
          >
            {loading ? 'APLICANDO CAMBIOS...' : 'APLICAR EDICIÓN IA (2 CRD)'}
          </button>
        </div>

      </div>
    </div>
  );
}

const activeTab = { flex: 1, backgroundColor: '#fff', color: '#000', border: 'none', padding: '12px', borderRadius: '8px', fontWeight: 'bold', fontSize: '11px' };
const inactiveTab = { flex: 1, backgroundColor: '#0a0a0a', color: '#444', border: '1px solid #1c1c1c', padding: '12px', borderRadius: '8px', fontWeight: 'bold', fontSize: '11px' };
const toolBtn = { backgroundColor: '#000', color: '#fff', border: '1px solid #222', padding: '12px', borderRadius: '10px', fontSize: '10px', fontWeight: 'bold', cursor: 'pointer' };
